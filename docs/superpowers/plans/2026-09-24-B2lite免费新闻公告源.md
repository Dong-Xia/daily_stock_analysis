# B2-lite 免费新闻/公告源接入 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 搜索引擎链全灭时 agent 新闻检索自动降级到东财免费个股新闻,并新增 `get_stock_announcements` 工具拉取个股法定公告(一手信源,服务 capital_forensics 监管痕迹链)。

**Architecture:** `data_provider/free_news_adapter.py`(新,无状态 fail-open,akshare 国内直连零 key)→ 工具层两处消费:`search_stock_news` handler 的 error 路径降级 + 新工具 `get_stock_announcements`(进 `ALL_SEARCH_TOOLS` 自动注册)。零新配置。

**Tech Stack:** Python 3.11(`.venv/bin/python`)/ akshare 1.18.64(`stock_news_em`、`stock_individual_notice_report`,接口形态已真机验证)/ unittest+pytest。

**Spec:** `docs/superpowers/specs/2026-09-24-B2lite免费新闻公告源-design.md`

**⚠️ 仓库硬规则:全程不执行 `git commit` / `git push`。测试命令一律用 `.venv/bin/python -m pytest`(系统 python3 是 3.9,会误报);py_compile 用 `/opt/homebrew/bin/python3.11`。**

**已核实锚点:**
- `src/agent/tools/search_tools.py`:`_handle_search_stock_news`(handler,:20-53)现逻辑——`is_available` False → 直接返回 error;成功 → 返回 results 数组(title/snippet/url/source/published_date);`search_stock_news_tool = ToolDefinition(...)`(:58-77,parameters 为 ToolParameter 列表,category="search");文件 :149 有 `ALL_SEARCH_TOOLS = [search_stock_news_tool, ...]` 列表,被 `src/agent/factory.py:162` 消费(新工具加入该列表即自动注册)
- `ToolDefinition(name, description, parameters, handler, category)` / `ToolParameter(name, type, description)`(registry.py:36-42)
- 接口实测:`ak.stock_news_em(symbol='600519')` 0.33s,列=关键词/新闻标题/新闻内容/发布时间/文章来源/新闻链接;`ak.stock_individual_notice_report(security='600519', symbol='全部')` 列=代码/名称/公告标题/公告类型/公告日期(date 对象)/网址;`ak.stock_notice_report` 弃用(symbol 是类别非代码)
- 既有回归测试:`tests/test_search_news_freshness.py`、`tests/test_search_performance.py`、`tests/test_data_tools_get_stock_info.py`(不可破坏)
- 工作区有用户自己的未提交文件(`src/services/sector_to_stock_selector.py`、`tests/test_sector_to_stock_selector.py`)——**任何任务不得触碰、不得 git add**

---

### Task 1: FreeNewsAdapter

**Files:**
- Create: `data_provider/free_news_adapter.py`
- Create: `tests/test_free_news_adapter.py`

- [ ] **Step 1.1: 写失败测试**

创建 `tests/test_free_news_adapter.py`:

```python
# -*- coding: utf-8 -*-
"""B2-lite 免费新闻/公告源 adapter 测试(全 mock,无网络)。"""

import os
import sys
import unittest
from datetime import date
from unittest.mock import patch

import pandas as pd

sys.path.insert(0, os.path.abspath(os.path.join(os.path.dirname(__file__), "..")))

from data_provider.free_news_adapter import FreeNewsAdapter

ADAPTER = FreeNewsAdapter()

NEWS_DF = pd.DataFrame({
    "关键词": ["600519"] * 2,
    "新闻标题": ["茅台发布中报", "茅台召开业绩说明会"],
    "新闻内容": ["净利润 445 亿。" + "x" * 300, "会议将于下周召开。"],
    "发布时间": ["2026-08-15 10:11:51", "2026-08-14 09:00:00"],
    "文章来源": ["东方财富网", "证券时报"],
    "新闻链接": ["https://e.com/a1", "https://e.com/a2"],
})

NOTICE_DF = pd.DataFrame({
    "代码": ["600519"] * 3,
    "名称": ["贵州茅台"] * 3,
    "公告标题": ["减持计划公告", "回购注销公告", "半年度报告摘要"],
    "公告类型": ["其他", "其他", "半年度报告摘要"],
    "公告日期": [date(2026, 9, 1), date(2026, 8, 20), date(2026, 8, 15)],
    "网址": ["https://d.com/n1", "https://d.com/n2", "https://d.com/n3"],
})


class TestGetStockNews(unittest.TestCase):
    def test_column_mapping_and_snippet_cap(self) -> None:
        with patch("akshare.stock_news_em", return_value=NEWS_DF):
            payload = ADAPTER.get_stock_news("600519")
        self.assertEqual(payload["status"], "ok")
        self.assertEqual(len(payload["items"]), 2)
        first = payload["items"][0]
        self.assertEqual(first["title"], "茅台发布中报")
        self.assertEqual(len(first["snippet"]), 200)  # 截断到 200 字
        self.assertEqual(first["source"], "东方财富网")
        self.assertEqual(first["published_date"], "2026-08-15")

    def test_empty_df_is_ok(self) -> None:
        with patch("akshare.stock_news_em", return_value=pd.DataFrame()):
            payload = ADAPTER.get_stock_news("600519")
        self.assertEqual(payload["status"], "ok")
        self.assertEqual(payload["items"], [])

    def test_exception_failed(self) -> None:
        with patch("akshare.stock_news_em", side_effect=TimeoutError("t")):
            payload = ADAPTER.get_stock_news("600519")
        self.assertEqual(payload["status"], "failed")
        self.assertTrue(any("stock_news_em" in e for e in payload["errors"]))

    def test_missing_title_column_failed(self) -> None:
        with patch("akshare.stock_news_em", return_value=pd.DataFrame({"任意": [1]})):
            payload = ADAPTER.get_stock_news("600519")
        self.assertEqual(payload["status"], "failed")


class TestGetStockAnnouncements(unittest.TestCase):
    def test_mapping_and_sort_desc(self) -> None:
        with patch("akshare.stock_individual_notice_report", return_value=NOTICE_DF) as m:
            payload = ADAPTER.get_stock_announcements("600519", days=90)
        self.assertEqual(payload["status"], "ok")
        self.assertEqual(len(payload["items"]), 3)
        self.assertEqual(payload["items"][0]["title"], "减持计划公告")  # 日期降序
        self.assertEqual(payload["items"][0]["notice_type"], "其他")
        self.assertEqual(payload["items"][0]["date"], "2026-09-01")
        # 日期参数以紧凑格式传入
        kwargs = m.call_args.kwargs
        self.assertIn("begin_date", kwargs)

    def test_keyword_filter(self) -> None:
        with patch("akshare.stock_individual_notice_report", return_value=NOTICE_DF):
            payload = ADAPTER.get_stock_announcements("600519", keyword="回购")
        self.assertEqual(payload["status"], "ok")
        self.assertEqual([i["title"] for i in payload["items"]], ["回购注销公告"])

    def test_days_window_filters_old(self) -> None:
        # 以公告列表里最新日期(2026-09-01)为锚难做确定性窗口断言,改用单行远期数据验证过滤逻辑
        df = pd.DataFrame({
            "代码": ["600519"], "名称": ["贵州茅台"],
            "公告标题": ["三年前的旧公告"], "公告类型": ["其他"],
            "公告日期": [date(2020, 1, 1)], "网址": ["https://d.com/old"],
        })
        with patch("akshare.stock_individual_notice_report", return_value=df):
            payload = ADAPTER.get_stock_announcements("600519", days=90)
        self.assertEqual(payload["status"], "ok")
        self.assertEqual(payload["items"], [])  # 窗口外被过滤

    def test_date_params_fail_retries_without_dates(self) -> None:
        # 带日期调用失败 → 不带日期重试成功 → 本地过滤
        with patch("akshare.stock_individual_notice_report",
                   side_effect=[TypeError("bad kw"), NOTICE_DF]) as m:
            payload = ADAPTER.get_stock_announcements("600519", days=3650)
        self.assertEqual(payload["status"], "ok")
        self.assertEqual(m.call_count, 2)
        self.assertNotIn("begin_date", m.call_args_list[1].kwargs)

    def test_all_fail_failed(self) -> None:
        with patch("akshare.stock_individual_notice_report", side_effect=TypeError("bad kw")):
            payload = ADAPTER.get_stock_announcements("600519")
        self.assertEqual(payload["status"], "failed")


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 1.2: 运行确认失败**

Run: `.venv/bin/python -m pytest tests/test_free_news_adapter.py -v`
Expected: ERROR(ModuleNotFoundError: data_provider.free_news_adapter)

- [ ] **Step 1.3: 实现 `data_provider/free_news_adapter.py`**

```python
# -*- coding: utf-8 -*-
"""
免费新闻/公告源(akshare,国内直连,无 API key,fail-open)。

- get_stock_news: ak.stock_news_em — 东方财富个股新闻
- get_stock_announcements: ak.stock_individual_notice_report — 东方财富公告大全(个股,法定信源)

所有异常吞入 errors,不向调用方抛出。
"""

from __future__ import annotations

import logging
from datetime import date, timedelta
from typing import Any, Dict, List, Optional

logger = logging.getLogger(__name__)


def _find_col(df: pd.DataFrame_like, keys, exclude=()):
    """列名关键词定位(先全等再子串,可排除),应对 akshare 列名漂移。"""
    for col in df.columns:
        if str(col) in keys:
            return col
    for col in df.columns:
        col_str = str(col)
        if any(k in col_str for k in keys) and not any(e in col_str for e in exclude):
            return col
    return None


def _safe_str(value) -> Optional[str]:
    if value is None:
        return None
    text = str(value).strip()
    return text or None


def _safe_date_str(value) -> Optional[str]:
    """日期列可能是 date/datetime/str,统一为 YYYY-MM-DD;解析失败取前 10 字符。"""
    try:
        import pandas as pd

        if pd.isna(value):
            return None
        return pd.to_datetime(value).strftime("%Y-%m-%d")
    except Exception:
        text = _safe_str(value)
        return text[:10] if text else None


class FreeNewsAdapter:
    """akshare 免费新闻/公告源(无状态,fail-open)。"""

    def get_stock_news(self, stock_code: str, max_items: int = 10) -> Dict[str, Any]:
        result: Dict[str, Any] = {"status": "failed", "items": [], "errors": []}
        try:
            import akshare as ak

            df = ak.stock_news_em(symbol=stock_code)
        except Exception as exc:
            result["errors"].append(f"stock_news_em:{type(exc).__name__}")
            return result
        if df is None or df.empty:
            result["status"] = "ok"
            return result
        title_col = _find_col(df, ["新闻标题", "标题"])
        content_col = _find_col(df, ["新闻内容", "内容"])
        time_col = _find_col(df, ["发布时间", "时间"])
        source_col = _find_col(df, ["文章来源", "来源"])
        link_col = _find_col(df, ["新闻链接", "链接"])
        if title_col is None:
            result["errors"].append("stock_news_em:missing_title_column")
            return result
        items: List[Dict[str, Any]] = []
        for _, row in df.head(max(int(max_items), 1)).iterrows():
            snippet = _safe_str(row[content_col]) if content_col else None
            items.append({
                "title": _safe_str(row[title_col]),
                "snippet": snippet[:200] if snippet else None,
                "url": _safe_str(row[link_col]) if link_col else None,
                "source": _safe_str(row[source_col]) if source_col else None,
                "published_date": _safe_date_str(row[time_col]) if time_col else None,
            })
        result["status"] = "ok"
        result["items"] = items
        return result

    def get_stock_announcements(
        self,
        stock_code: str,
        days: int = 90,
        keyword: Optional[str] = None,
    ) -> Dict[str, Any]:
        result: Dict[str, Any] = {"status": "failed", "items": [], "errors": []}
        end = date.today()
        begin = end - timedelta(days=max(1, int(days)))

        def _call(with_dates: bool):
            import akshare as ak

            if with_dates:
                return ak.stock_individual_notice_report(
                    security=stock_code,
                    symbol="全部",
                    begin_date=begin.strftime("%Y%m%d"),
                    end_date=end.strftime("%Y%m%d"),
                )
            return ak.stock_individual_notice_report(security=stock_code, symbol="全部")

        df = None
        try:
            df = _call(with_dates=True)
        except Exception as exc:
            result["errors"].append(f"stock_individual_notice_report:{type(exc).__name__}")
            logger.debug("公告接口带日期调用失败,尝试无日期重试: %s", exc)
            try:
                df = _call(with_dates=False)
            except Exception as exc2:
                result["errors"].append(f"stock_individual_notice_report_retry:{type(exc2).__name__}")
                return result
        if df is None or df.empty:
            result["status"] = "ok"
            return result
        title_col = _find_col(df, ["公告标题", "标题"])
        type_col = _find_col(df, ["公告类型", "类型"])
        date_col = _find_col(df, ["公告日期", "日期"])
        url_col = _find_col(df, ["网址", "链接"])
        if title_col is None or date_col is None:
            result["errors"].append("stock_individual_notice_report:missing_columns")
            return result
        kw = (keyword or "").strip().lower()
        window_begin = begin.isoformat()
        items: List[Dict[str, Any]] = []
        for _, row in df.iterrows():
            title = _safe_str(row[title_col])
            if not title:
                continue
            if kw and kw not in title.lower():
                continue
            date_str = _safe_date_str(row[date_col])
            if date_str and date_str < window_begin:
                continue
            items.append({
                "date": date_str,
                "notice_type": _safe_str(row[type_col]) if type_col else None,
                "title": title,
                "url": _safe_str(row[url_col]) if url_col else None,
            })
        items.sort(key=lambda r: r.get("date") or "", reverse=True)
        result["status"] = "ok"
        result["items"] = items
        return result


FREE_NEWS_ADAPTER = FreeNewsAdapter()


def get_free_news_adapter() -> FreeNewsAdapter:
    """共享单例(与 search_service.get_search_service 风格一致)。"""
    return FREE_NEWS_ADAPTER
```

**实现注意**:上面 `_find_col` 签名里的 `df: pd.DataFrame_like` 是伪类型——实际写成 `df` 不加注解,或 `from typing import Any` 后用 `df: Any`。模块顶部**不要** import pandas(仅在函数内按需 import,保持模块导入零依赖);`_safe_date_str` 内已按需 import。

- [ ] **Step 1.4: 运行确认通过**

Run: `.venv/bin/python -m pytest tests/test_free_news_adapter.py -v`
Expected: 9 passed

(注:`test_days_window_filters_old` 依赖"窗口外过滤"走本地 date_str < window_begin 分支——2020-01-01 < 今天-90 天,必然过滤。)

---

### Task 2: search_stock_news 免费源降级

**Files:**
- Modify: `src/agent/tools/search_tools.py`(`_handle_search_stock_news` 重写,:20-53)
- Create: `tests/test_search_tools_free_fallback.py`

- [ ] **Step 2.1: 写失败测试**

创建 `tests/test_search_tools_free_fallback.py`:

```python
# -*- coding: utf-8 -*-
"""B2-lite:search_stock_news 免费源降级 + get_stock_announcements 工具测试(全 mock)。"""

import os
import sys
import unittest
from types import SimpleNamespace
from unittest.mock import Mock, patch

sys.path.insert(0, os.path.abspath(os.path.join(os.path.dirname(__file__), "..")))

from src.agent.tools.search_tools import _handle_search_stock_news, _handle_get_stock_announcements

FREE_NEWS_OK = {
    "status": "ok",
    "items": [
        {"title": "茅台发布中报", "snippet": "净利润 445 亿", "url": "https://e.com/a1",
         "source": "东方财富网", "published_date": "2026-08-15"},
    ],
    "errors": [],
}


def _engine_service(available=True, success=True):
    """构造假 SearchService。"""
    resp = SimpleNamespace(
        query="q", success=success, error_message=None if success else "引擎失败",
        provider="mock_engine", results=[SimpleNamespace(
            title="t", snippet="s", url="u", source="src", published_date="2026-08-15")],
    )
    service = SimpleNamespace(
        is_available=available,
        search_stock_news=Mock(return_value=resp),
    )
    return service


class TestSearchStockNewsFallback(unittest.TestCase):
    def test_engine_unavailable_falls_back(self) -> None:
        adapter = Mock()
        adapter.get_stock_news.return_value = FREE_NEWS_OK
        with patch("src.agent.tools.search_tools._get_search_service",
                   return_value=_engine_service(available=False)), \
             patch("data_provider.free_news_adapter.get_free_news_adapter", return_value=adapter):
            out = _handle_search_stock_news("600519", "贵州茅台")
        self.assertTrue(out["success"])
        self.assertTrue(out["fallback"])
        self.assertEqual(out["provider"], "eastmoney_free")
        self.assertEqual(out["results"][0]["title"], "茅台发布中报")

    def test_engine_success_no_fallback(self) -> None:
        adapter = Mock()
        with patch("src.agent.tools.search_tools._get_search_service",
                   return_value=_engine_service(available=True, success=True)), \
             patch("data_provider.free_news_adapter.get_free_news_adapter", return_value=adapter):
            out = _handle_search_stock_news("600519", "贵州茅台")
        self.assertTrue(out["success"])
        self.assertNotIn("fallback", out)
        adapter.get_stock_news.assert_not_called()

    def test_engine_fail_falls_back(self) -> None:
        adapter = Mock()
        adapter.get_stock_news.return_value = FREE_NEWS_OK
        with patch("src.agent.tools.search_tools._get_search_service",
                   return_value=_engine_service(available=True, success=False)), \
             patch("data_provider.free_news_adapter.get_free_news_adapter", return_value=adapter):
            out = _handle_search_stock_news("600519", "贵州茅台")
        self.assertTrue(out["fallback"])

    def test_both_fail_structured_error(self) -> None:
        adapter = Mock()
        adapter.get_stock_news.return_value = {"status": "failed", "items": [], "errors": ["stock_news_em:TimeoutError"]}
        with patch("src.agent.tools.search_tools._get_search_service",
                   return_value=_engine_service(available=False)), \
             patch("data_provider.free_news_adapter.get_free_news_adapter", return_value=adapter):
            out = _handle_search_stock_news("600519", "贵州茅台")
        self.assertFalse(out["success"])
        self.assertIn("fallback_error", out)

    def test_fallback_empty_still_structured_error(self) -> None:
        adapter = Mock()
        adapter.get_stock_news.return_value = {"status": "ok", "items": [], "errors": []}
        with patch("src.agent.tools.search_tools._get_search_service",
                   return_value=_engine_service(available=False)), \
             patch("data_provider.free_news_adapter.get_free_news_adapter", return_value=adapter):
            out = _handle_search_stock_news("600519", "贵州茅台")
        self.assertFalse(out["success"])


class TestGetStockAnnouncementsTool(unittest.TestCase):
    def test_a_share_ok(self) -> None:
        adapter = Mock()
        adapter.get_stock_announcements.return_value = {
            "status": "ok",
            "items": [{"date": "2026-09-01", "notice_type": "其他",
                       "title": "减持计划公告", "url": "https://d.com/n1"}],
            "errors": [],
        }
        with patch("data_provider.free_news_adapter.get_free_news_adapter", return_value=adapter):
            out = _handle_get_stock_announcements("600519", days=90, keyword="减持")
        self.assertTrue(out["success"])
        self.assertEqual(out["count"], 1)
        adapter.get_stock_announcements.assert_called_once_with("600519", days=90, keyword="减持")

    def test_non_a_share_unsupported(self) -> None:
        adapter = Mock()
        with patch("data_provider.free_news_adapter.get_free_news_adapter", return_value=adapter):
            out = _handle_get_stock_announcements("hk00700")
        self.assertFalse(out["success"])
        adapter.get_stock_announcements.assert_not_called()

    def test_adapter_failed(self) -> None:
        adapter = Mock()
        adapter.get_stock_announcements.return_value = {"status": "failed", "items": [], "errors": ["x:E"]}
        with patch("data_provider.free_news_adapter.get_free_news_adapter", return_value=adapter):
            out = _handle_get_stock_announcements("600519")
        self.assertFalse(out["success"])


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2.2: 运行确认失败**

Run: `.venv/bin/python -m pytest tests/test_search_tools_free_fallback.py -v`
Expected: ERROR/FAIL(`_handle_get_stock_announcements` 不存在;降级路径不存在)

- [ ] **Step 2.3: 重写 handler 并加新工具(`src/agent/tools/search_tools.py`)**

(a) 把 `_handle_search_stock_news` 整体替换为:

```python
def _handle_search_stock_news(stock_code: str, stock_name: str) -> dict:
    """Search latest news for a stock; falls back to free Eastmoney news on engine failure."""
    service = _get_search_service()

    engine_error: Optional[str] = None
    if not service.is_available:
        engine_error = "No search engine available (no API keys configured)"
    else:
        response = service.search_stock_news(stock_code, stock_name, max_results=5)
        if response.success:
            return {
                "query": response.query,
                "provider": response.provider,
                "success": True,
                "results_count": len(response.results),
                "results": [
                    {
                        "title": r.title,
                        "snippet": r.snippet,
                        "url": r.url,
                        "source": r.source,
                        "published_date": r.published_date,
                    }
                    for r in response.results
                ],
            }
        engine_error = response.error_message or "search engine failed"

    # 免费源降级(仅在引擎不可用/失败时;东财个股新闻,国内直连)
    from data_provider.free_news_adapter import get_free_news_adapter

    free = get_free_news_adapter().get_stock_news(stock_code)
    if free.get("status") == "ok" and free.get("items"):
        return {
            "query": f"{stock_code} {stock_name}".strip(),
            "provider": "eastmoney_free",
            "fallback": True,
            "note": "搜索引擎不可用,已降级为东方财富免费个股新闻(覆盖面低于全网搜索,结论措辞请留余地)",
            "success": True,
            "results_count": len(free["items"]),
            "results": free["items"],
        }
    fallback_error = (
        "; ".join(free.get("errors", [])) or "free source returned no items"
    )
    return {
        "query": f"{stock_code} {stock_name}".strip(),
        "success": False,
        "error": engine_error,
        "fallback_error": fallback_error,
    }
```

(b) 在 `search_comprehensive_intel` 定义之后(文件后部)加新 handler 与 ToolDefinition:

```python
# ============================================================
# get_stock_announcements (B2-lite 免费法定公告源)
# ============================================================

def _handle_get_stock_announcements(stock_code: str, days: int = 90, keyword: str = "") -> dict:
    """Fetch statutory announcements for an A-share stock (primary-source data)."""
    code = (stock_code or "").strip()
    if not code or not code[:1].isdigit() or code[:1] not in ("6", "0", "3", "4", "8"):
        return {"success": False, "error": "announcements only support A-shares"}

    from data_provider.free_news_adapter import get_free_news_adapter

    payload = get_free_news_adapter().get_stock_announcements(
        code, days=days, keyword=(keyword or "").strip() or None
    )
    if payload.get("status") != "ok":
        return {
            "success": False,
            "error": "; ".join(payload.get("errors", [])) or "announcement fetch failed",
        }
    return {
        "success": True,
        "stock_code": code,
        "days": days,
        "keyword": (keyword or "").strip() or None,
        "count": len(payload.get("items", [])),
        "announcements": payload.get("items", []),
        "note": "法定公告一手信源(东方财富公告大全);适用于减持/质押/回购/问询函/举牌等监管痕迹核验",
    }


get_stock_announcements_tool = ToolDefinition(
    name="get_stock_announcements",
    description="Fetch statutory announcements (法定公告) for an A-share stock: 减持/质押/回购/问询函/举牌/"
                "定期报告 etc. Primary-source data from Eastmoney notice center. "
                "Use for regulatory-trace verification (监管痕迹核验).",
    parameters=[
        ToolParameter(
            name="stock_code",
            type="string",
            description="A-share stock code, e.g., '600519'",
        ),
        ToolParameter(
            name="days",
            type="integer",
            description="Lookback window in days, default 90",
        ),
        ToolParameter(
            name="keyword",
            type="string",
            description="Optional keyword filter on announcement title (e.g., '减持')",
        ),
    ],
    handler=_handle_get_stock_announcements,
    category="search",
)
```

(c) `ALL_SEARCH_TOOLS` 列表(:149 附近)追加 `get_stock_announcements_tool`(import/同文件内直接加列表项)。

- [ ] **Step 2.4: 运行确认通过 + 既有回归**

Run:
```bash
.venv/bin/python -m pytest tests/test_search_tools_free_fallback.py tests/test_free_news_adapter.py tests/test_search_news_freshness.py tests/test_search_performance.py -v
```
Expected: 8+9+既有全 passed。若 `test_search_news_freshness`/`test_search_performance` 有失败,先确认是否既有失败(在 main 基线跑同样命令对照),新失败必须修。

---

### Task 3: 消费端资产同步

**Files:**
- Modify: `strategies/capital_forensics.yaml`(第四步开头)
- Modify: `docs/资金筹码取证方法论.md`(第 10 章 ✅ 段末)
- Modify: `docs/资金筹码取证使用指南.md`(FAQ 增补)
- Modify: `docs/CHANGELOG.md`(`[Unreleased]` 节尾)

- [ ] **Step 3.1: YAML 第四步指引**

`strategies/capital_forensics.yaml` 第四步标题行 `## 第四步:监管痕迹交叉验证(一票否决层)` 之后、`使用 \`search_stock_news\` 检索近 6 个月公告类信息,按三条链匹配:` 之前,插入:

```
  优先使用 `get_stock_announcements` 获取法定公告（一手信源：减持/质押/回购/问询函/举牌），
  按链 A/B/C 匹配公告标题；`search_stock_news` 作为补充（新闻转述，注意区分一手与二手信源，
  引擎不可用时它会自动降级为东财免费新闻）。
```

(原 `使用 \`search_stock_news\` 检索…` 行保留或改为"补充检索"措辞,实现时保持上下文通顺。)

- [ ] **Step 3.2: 方法论第 10 章 ✅ 段末追加**

```markdown
- ✅(B2-lite 已接入,2026-09,免费无 key 国内直连)`stock_news_em`(东财个股新闻,search_stock_news
  引擎链失败时自动降级)与 `stock_individual_notice_report`(东财个股法定公告,agent 工具
  get_stock_announcements,监管痕迹链一手信源)。
```

- [ ] **Step 3.3: 使用指南 FAQ 增补**

`docs/资金筹码取证使用指南.md` 的 FAQ 节末尾追加:

```markdown
**Q:输出里出现 "fallback": true / "eastmoney_free" 是什么?**
搜索引擎链不可用(未配 key 且公共实例被网络阻断)时,新闻检索自动降级为东方财富免费个股新闻——数据可用但覆盖面低于全网搜索,AI 会据此在结论措辞上留余地。公告核验请认准 `get_stock_announcements`(法定公告一手信源,不受搜索引擎状态影响)。
```

- [ ] **Step 3.4: CHANGELOG 两条**

```markdown
- [新功能] 免费新闻/公告源: search_stock_news 引擎链失败自动降级东财个股新闻;新工具 get_stock_announcements 拉取个股法定公告(监管痕迹一手信源,零 key 国内直连)
- [文档] 资金筹码取证方法论/使用指南同步免费源说明
```

- [ ] **Step 3.5: 验证**

```bash
.venv/bin/python -c "import yaml; d=yaml.safe_load(open('strategies/capital_forensics.yaml')); ins=d['instructions']; assert 'get_stock_announcements' in ins and '一手信源' in ins; print('yaml OK')"
.venv/bin/python -c "
from src.agent.tools.search_tools import ALL_SEARCH_TOOLS
names = [t.name for t in ALL_SEARCH_TOOLS]
assert 'get_stock_announcements' in names and 'search_stock_news' in names, names
print('tools OK:', names)
"
grep -c "B2-lite" docs/资金筹码取证方法论.md   # ≥1
```

---

### Task 4: 终检与真机验收

**Files:** 只读验证(真机若发现接口签名/列名漂移,仅允许修 `free_news_adapter.py` 内的 kwargs/关键词并同步 fixture)。

- [ ] **Step 4.1: 静态与离线**

```bash
/opt/homebrew/bin/python3.11 -m py_compile data_provider/free_news_adapter.py src/agent/tools/search_tools.py
.venv/bin/python -m flake8 --select=E9,F63,F7,F82 data_provider/free_news_adapter.py src/agent/tools/search_tools.py
.venv/bin/python -m pytest tests/test_free_news_adapter.py tests/test_search_tools_free_fallback.py tests/test_search_news_freshness.py tests/test_search_performance.py tests/test_identity_adapter.py -q
./scripts/ci_gate.sh deterministic
```

- [ ] **Step 4.2: 真机·新闻降级(当前引擎即不可用状态)**

```bash
.venv/bin/python -c "
from src.agent.tools.search_tools import _handle_search_stock_news
out = _handle_search_stock_news('600519', '贵州茅台')
print('success:', out.get('success'), '| fallback:', out.get('fallback'), '| provider:', out.get('provider'))
print('首条:', (out.get('results') or [{}])[0].get('title'))
"
```

Expected: `success: True | fallback: True | provider: eastmoney_free`,首条为真实茅台新闻。

- [ ] **Step 4.3: 真机·公告工具**

```bash
.venv/bin/python -c "
from src.agent.tools.search_tools import _handle_get_stock_announcements
out = _handle_get_stock_announcements('600519', days=90, keyword='回购')
print('success:', out.get('success'), '| count:', out.get('count'))
for a in (out.get('announcements') or [])[:3]:
    print(' ', a.get('date'), a.get('notice_type'), a.get('title'))
"
```

Expected: success True;count ≥0(窗口内无回购公告返回 0 也正常);去掉 keyword 再跑一次应有公告列表(半年度报告摘要等)。

- [ ] **Step 4.4: 交付说明**

按仓库规则输出:改了什么/为什么/验证情况(含真机)/未验证项(未跑完整 /ask 流程——工具层已单测+真机直测;8000 服务需重启才有新工具)/风险点(东财接口无 SLA,列名漂移由关键词防御;免费新闻无全网覆盖)/回滚方式(删 1 新文件+还原 search_tools.py+3 个文档)。

---

## Self-Review 记录(计划编写时完成)

1. **Spec 覆盖**:adapter(§3.1)→Task 1;降级(§3.2)→Task 2a;新工具(§3.3)→Task 2b/c;消费端四资产(§3.4)→Task 3;测试验收(§5)→Task 1/2 测试+Task 4。§4 错误处理分布在两任务的 fail-open 与结构化 error 中。无缺口。
2. **占位符**:无 TBD;Task 1 的 `_find_col` 伪类型注解已显式标注处理方式。
3. **命名一致**:`FreeNewsAdapter.get_stock_news/get_stock_announcements`、`get_free_news_adapter`、`_handle_get_stock_announcements`、`get_stock_announcements_tool`、`ALL_SEARCH_TOOLS` 各处引用一致;patch 目标 `data_provider.free_news_adapter.get_free_news_adapter` 与函数内局部 import 的解析方式匹配(局部 import 每次调用解析模块属性,patch 生效)。
4. **已知注意点**:Task 2c 修改 `ALL_SEARCH_TOOLS` 时该列表可能含其他工具(如 comprehensive_intel),只追加不删改;Task 4.2 依赖当前环境引擎不可用(正是要复现的状态),若届时用户已配 key,改用 `patch("src.agent.tools.search_tools._get_search_service", return_value=SimpleNamespace(is_available=False))` 方式验证降级分支。
5. **实施期修订(2026-09-24,Task 1 质量审查)**:①测试时间炸弹——`test_mapping_and_sort_desc`/`test_keyword_filter` 的固定 fixture(2026-08/09)配真实 `date.today()`,分别于 2026-11-14/11-19 起失败,修法=两处改 `days=36500`+注释(随 Task 2 落地);②`_safe_date_str` 解析失败分支由 `text[:10]` 改为返回 `None`(防伪日期进窗口比较,对齐 `_safe_datetime` 语义);③Task 2 新闻降级路径补非 A 股门控(与公告工具同款前缀检查,返回语义化 unsupported 而非 akshare 报错);④公告工具返回列表设上限 100 条,截断时附 `"truncated": true`(no silent caps);⑤公告无日期重试路径会翻全量历史页(akshare 按 total_hits/100 翻页),属降级路径可接受,上限④即为此兜底。
6. **实施期修订(2026-09-24,Task 2 质量审查)**:①**前缀门控升级**——裸前缀 (6/0/3/4/8) 与仓库市场判定矛盾(实证:`SH600519` 被拒、北交所现行 92 开头被拒、`00700`/`600519.SH` 反被放过),Task 4 改为 handler 内懒加载 `from data_provider.base import normalize_stock_code` + `_market_tag(normalize_stock_code(code))=="cn"` 门控(私有跨模块导入为轻度权衡,与已懒加载 free_news_adapter 的依赖方向一致);②公告 note 补"仅含标题/类型/日期,金额比例以公告正文为准";truncated 补"可用 keyword/days 收窄"提示;③门控错误补 `"retriable": False`(对齐 get_realtime_quote,命中 runner 的 non-retriable 缓存);④补测试:引擎/降级两路径 results 的 item schema 一致性、门控边界(SH600519/920748/600519.SH/00700)、fallback_error 含 "no items" 断言强化。
